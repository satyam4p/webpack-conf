import React from "react";

/** todo template for pills to be used for status and Label of task */
const Pills=({type, label})=>{

    return(
        <div>

        </div>
    )

}
export default Pills;